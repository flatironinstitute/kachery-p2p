{
  "name": "kachery-p2p-daemon",
  "version": "0.4.9",
  "engines": {
    "node": ">=12.0.0"
  },
  "description": "Peer-to-peer content-addressable file sharing",
  "bin": "./lib/cli.js",
  "scripts": {
    "test": "echo \"No tests\" && exit 0",
    "babel": "babel src --out-dir lib --ignore node_modules",
    "publish-dry": "rm -r lib && npm run babel && cp src/schema/*.json lib/schema/ && npm publish --dry-run",
    "publish-go": "rm -r lib && npm run babel && cp src/schema/*.json lib/schema/ && npm publish"
  },
  "repository": "https://github.com/flatironinstitute/kachery_p2p",
  "author": "Jeremy Magland",
  "license": "Apache-2.0",
  "dependencies": {
    "axios": "^0.19.2",
    "express": "^4.17.1",
    "ip": "^1.1.5",
    "json-socket": "^0.3.0",
    "jsonschema": "^1.2.6",
    "ws": "^7.3.1",
    "yargs": "^15.3.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.4",
    "@babel/core": "^7.10.4",
    "@babel/plugin-proposal-class-properties": "^7.10.4",
    "@babel/preset-env": "^7.10.4"
  }
}
