{
    "$id": "/RequestToNodeReceivedMessage",
    "type": "object",
    "properties": {
        "type": {"const": "requestToNodeReceived"},
        "requestId": {"$ref": "/MessageId"}
    },
    "required": ["type", "requestId"],
    "additionalProperties": false
}