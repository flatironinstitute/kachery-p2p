const PROTOCOL_VERSION = 'kachery-p2p-0.4.18';
const DAEMON_VERSION = 'kachery-p2p-0.4.26';

export const protocolVersion = () => {
    return PROTOCOL_VERSION;
}

export const daemonVersion = () => {
    return DAEMON_VERSION;
}